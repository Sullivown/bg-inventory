extends layout 

block content
	h1= title 

	h3 Boardgames:
	
	ul
		each designer in designer_list 
			li
				a(href=designer.url) #{designer.name}
		
		else
			li There are no designers!
	
	hr
	a(href='/designer/create') Create New Designer