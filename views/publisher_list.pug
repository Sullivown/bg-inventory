extends layout 

block content
	h1= title 

	ul
		each publisher in publisher_list 
			li
				a(href=publisher.url) #{publisher.name}
		
		else
			li There are no publishers!
	
	hr
	a(href='/publisher/create') Create New Publisher