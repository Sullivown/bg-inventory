extends layout 

block content 
	h1= title 

	h2= category.name

	if category_boardgames.length
		p This category is attributed to the following Boardgames:
		ul
			each boardgame in category_boardgames 
				li= boardgame.title
		
		p Deleting them will remove them from the categories field of all games listed above.
	
	else
		p This category is not attributed to any board games.
	
	hr
	p Are you sure you want to delete this category?

	form(method='POST' action='')
		div.form-group
			input#categoryid.form-control(type='hidden',name='categoryid', required='true', value=category._id )

		button.btn.btn-primary(type='submit') Delete

	form(method='GET' action=category.url)
		button.btn.btn-primary(type='submit') Cancel